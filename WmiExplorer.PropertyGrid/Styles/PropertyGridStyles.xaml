<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:converters="clr-namespace:WmiExplorer.Presentation.Controls.PropertyGrid.Converters"
                    xmlns:local="clr-namespace:WmiExplorer.PropertyGrid">

    <!--  ================================================================  -->
    <!--  COMMON STYLES AND RESOURCES  -->
    <!--  ================================================================  -->

    <!--  Icons from SegoeFluentIcons  -->
    <!--  Reference: https://learn.microsoft.com/en-us/windows/apps/design/style/segoe-fluent-icons-font  -->
    <Style x:Key="PropertyGridIconStyle" TargetType="TextBlock">
        <Setter Property="HorizontalAlignment" Value="Center" />
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="Foreground" Value="{DynamicResource PropertyGridForegroundBrush}" />
        <Setter Property="FontFamily" Value="Segoe Fluent Icons, Segoe MDL2 Assets" />
        <Setter Property="FontSize" Value="16" />
    </Style>

    <!--  Common style for property text  -->
    <Style x:Key="PropertyTextBlockStyle" TargetType="TextBlock">
        <Setter Property="Margin" Value="0,2" />
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="Foreground" Value="{DynamicResource PropertyGridForegroundBrush}" />
        <Setter Property="TextTrimming" Value="CharacterEllipsis" />
    </Style>

    <!--  Common template for property description tooltips  -->
    <DataTemplate x:Key="PropertyDescriptionTooltip">
        <TextBlock MaxWidth="300" Text="{Binding Description}" TextWrapping="Wrap" />
    </DataTemplate>

    <!--  PropertyGrid TextBox ContextMenu Style  -->
    <ContextMenu x:Key="PropertyGridTextBoxContextMenu">
        <MenuItem Command="ApplicationCommands.Cut" Header="Cut">
            <MenuItem.Icon>
                <TextBlock Style="{StaticResource PropertyGridIconStyle}" Text="&#xE8C6;" />
            </MenuItem.Icon>
        </MenuItem>
        <MenuItem Command="ApplicationCommands.Copy" Header="Copy">
            <MenuItem.Icon>
                <TextBlock Style="{StaticResource PropertyGridIconStyle}" Text="&#xE8C8;" />
            </MenuItem.Icon>
        </MenuItem>
        <MenuItem Command="ApplicationCommands.Paste" Header="Paste">
            <MenuItem.Icon>
                <TextBlock Style="{StaticResource PropertyGridIconStyle}" Text="&#xE77F;" />
            </MenuItem.Icon>
        </MenuItem>
        <Separator />
        <MenuItem Command="ApplicationCommands.SelectAll" Header="Select All">
            <MenuItem.Icon>
                <TextBlock Style="{StaticResource PropertyGridIconStyle}" Text="&#xE8B3;" />
            </MenuItem.Icon>
        </MenuItem>
    </ContextMenu>

    <!--  TextBoxWithPlaceholder Style  -->
    <Style x:Key="PropertyGridTextBoxWithPlaceholder" BasedOn="{StaticResource {x:Type TextBox}}" TargetType="TextBox">
        <Setter Property="CaretBrush" Value="{DynamicResource PropertyGridForegroundBrush}" />
        <Setter Property="ContextMenu" Value="{StaticResource PropertyGridTextBoxContextMenu}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TextBox">
                    <Border x:Name="TextBorder"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{DynamicResource PrimaryAccentBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}">
                        <Grid>
                            <ScrollViewer x:Name="PART_ContentHost"
                                          Margin="0"
                                          Background="Transparent"
                                          Focusable="False" />
                            <TextBlock x:Name="PlaceholderText"
                                       Margin="{TemplateBinding Padding}"
                                       Padding="2,0,0,0"
                                       VerticalAlignment="Center"
                                       Foreground="{DynamicResource PropertyGridDisabledForegroundBrush}"
                                       IsHitTestVisible="False"
                                       Text="{TemplateBinding Tag}"
                                       Visibility="Collapsed" />
                        </Grid>
                    </Border>
                    <ControlTemplate.Triggers>
                        <!--  Apply margin only when ScrollViewer has scrollable content  -->
                        <MultiDataTrigger>
                            <MultiDataTrigger.Conditions>
                                <Condition Binding="{Binding ComputedVerticalScrollBarVisibility, ElementName=PART_ContentHost}" Value="Visible" />
                            </MultiDataTrigger.Conditions>
                            <Setter TargetName="PART_ContentHost" Property="Margin" Value="0,4,0,4" />
                        </MultiDataTrigger>
                        <MultiDataTrigger>
                            <MultiDataTrigger.Conditions>
                                <Condition Binding="{Binding ComputedHorizontalScrollBarVisibility, ElementName=PART_ContentHost}" Value="Visible" />
                            </MultiDataTrigger.Conditions>
                            <Setter TargetName="PART_ContentHost" Property="Margin" Value="0,4,0,4" />
                        </MultiDataTrigger>
                        <DataTrigger Binding="{Binding Text, RelativeSource={RelativeSource Self}}" Value="">
                            <Setter TargetName="PlaceholderText" Property="Visibility" Value="Visible" />
                            <Setter TargetName="TextBorder" Property="BorderBrush" Value="{Binding BorderBrush, RelativeSource={RelativeSource TemplatedParent}}" />
                        </DataTrigger>
                        <DataTrigger Binding="{Binding Text, RelativeSource={RelativeSource Self}}" Value="{x:Null}">
                            <Setter TargetName="PlaceholderText" Property="Visibility" Value="Visible" />
                            <Setter TargetName="TextBorder" Property="BorderBrush" Value="{Binding BorderBrush, RelativeSource={RelativeSource TemplatedParent}}" />
                        </DataTrigger>
                        <Trigger Property="IsFocused" Value="True">
                            <Setter TargetName="PlaceholderText" Property="Visibility" Value="Collapsed" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!--  ================================================================  -->
    <!--  TEXTBOX SEARCH BOX STYLE  -->
    <!--  ================================================================  -->
    <Style x:Key="SearchTextBoxStyle" TargetType="TextBox">
        <Setter Property="Background" Value="{DynamicResource PropertyGridBackgroundBrush}" />
        <Setter Property="BorderBrush" Value="{DynamicResource PropertyGridBorderBrush}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Padding" Value="26,4,26,4" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TextBox">
                    <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}">
                        <ScrollViewer x:Name="PART_ContentHost" Margin="{TemplateBinding Padding}" VerticalAlignment="Center" />
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!--  TextBox with Search Icon, Placeholder, and Clear Button (like TextBoxWithClearButton)  -->
    <Style x:Key="PropertyGridTextBoxWithClearButton" BasedOn="{StaticResource SearchTextBoxStyle}" TargetType="TextBox">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TextBox">
                    <Border x:Name="TextBorder"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}">
                        <Grid VerticalAlignment="Center">
                            <!--  Search Icon  -->
                            <Viewbox Width="16"
                                     Height="16"
                                     Margin="4,0,0,0"
                                     HorizontalAlignment="Left"
                                     VerticalAlignment="Center">
                                <Canvas Width="16" Height="16">
                                    <Ellipse Canvas.Left="1"
                                             Canvas.Top="2"
                                             Width="10"
                                             Height="10"
                                             Stroke="Gray"
                                             StrokeThickness="2" />
                                    <Line Stroke="Gray"
                                          StrokeThickness="2"
                                          X1="9"
                                          X2="14"
                                          Y1="10"
                                          Y2="15" />
                                </Canvas>
                            </Viewbox>
                            <!--  Text Content  -->
                            <ScrollViewer x:Name="PART_ContentHost"
                                          Margin="24,0,24,0"
                                          Background="Transparent"
                                          Focusable="false" />
                            <!--  Placeholder  -->
                            <TextBlock x:Name="PlaceholderText"
                                       Margin="24,0,24,0"
                                       Padding="0,0,0,0"
                                       VerticalAlignment="Center"
                                       Foreground="Gray"
                                       IsHitTestVisible="False"
                                       Text="{TemplateBinding Tag}"
                                       Visibility="Collapsed" />
                            <!--  Clear Button  -->
                            <Button x:Name="PART_ClearButton"
                                    Width="18"
                                    Height="18"
                                    Margin="0,0,4,0"
                                    Padding="0"
                                    HorizontalAlignment="Right"
                                    VerticalAlignment="Center"
                                    Background="Transparent"
                                    BorderBrush="Transparent"
                                    BorderThickness="0"
                                    Command="{Binding ClearSearchCommand, RelativeSource={RelativeSource AncestorType={x:Type local:PropertyGrid}}}"
                                    Cursor="Hand"
                                    Focusable="False"
                                    ToolTip="Clear">
                                <Path Data="M3,3 L9,9 M9,3 L3,9"
                                      Stroke="Gray"
                                      StrokeEndLineCap="Round"
                                      StrokeStartLineCap="Round"
                                      StrokeThickness="2" />
                            </Button>
                        </Grid>
                    </Border>
                    <ControlTemplate.Triggers>
                        <!--  Show placeholder and hide clear button if empty or null  -->
                        <DataTrigger Binding="{Binding Text, RelativeSource={RelativeSource Self}}" Value="">
                            <Setter TargetName="PlaceholderText" Property="Visibility" Value="Visible" />
                            <Setter TargetName="PART_ClearButton" Property="Visibility" Value="Collapsed" />
                        </DataTrigger>
                        <DataTrigger Binding="{Binding Text, RelativeSource={RelativeSource Self}}" Value="{x:Null}">
                            <Setter TargetName="PlaceholderText" Property="Visibility" Value="Visible" />
                            <Setter TargetName="PART_ClearButton" Property="Visibility" Value="Collapsed" />
                        </DataTrigger>
                        <!--  Show clear button only when Text is not empty or null  -->
                        <MultiDataTrigger>
                            <MultiDataTrigger.Conditions>
                                <Condition Binding="{Binding Text, RelativeSource={RelativeSource Self}}" Value="" />
                                <Condition Binding="{Binding Text, RelativeSource={RelativeSource Self}}" Value="{x:Null}" />
                            </MultiDataTrigger.Conditions>
                            <Setter TargetName="PART_ClearButton" Property="Visibility" Value="Collapsed" />
                        </MultiDataTrigger>
                        <Trigger Property="IsFocused" Value="True">
                            <Setter TargetName="PlaceholderText" Property="Visibility" Value="Collapsed" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!--  GridSplitter  -->
    <Style x:Key="PropertyGridGridSplitter" TargetType="GridSplitter">
        <Setter Property="Background" Value="{DynamicResource PropertyGridSecondaryBackgroundBrush}" />
        <Setter Property="Width" Value="Auto" />
        <Setter Property="Height" Value="4" />
        <Setter Property="Margin" Value="0,5,0,5" />
        <Setter Property="Focusable" Value="False" />
        <Setter Property="ShowsPreview" Value="True" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="GridSplitter">
                    <Border Background="{TemplateBinding Background}" />
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="{DynamicResource PropertyGridHoverBackgroundBrush}" />
                <Setter Property="Width" Value="Auto" />
            </Trigger>
            <Trigger Property="IsDragging" Value="True">
                <Setter Property="Background" Value="{DynamicResource PropertyGridHoverBackgroundBrush}" />
            </Trigger>
        </Style.Triggers>
    </Style>

    <!--  ================================================================  -->
    <!--  STYLES  -->
    <!--  ================================================================  -->

    <!--  Overlay style for scrollbars used in property grid  -->
    <Style x:Key="OverlayScrollBarStyle" TargetType="ScrollBar">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ScrollBar">
                    <Grid>
                        <Track Name="PART_Track"
                               IsDirectionReversed="true"
                               Maximum="{TemplateBinding Maximum}"
                               Minimum="{TemplateBinding Minimum}"
                               ViewportSize="{TemplateBinding ViewportSize}"
                               Value="{TemplateBinding Value}">
                            <Track.Thumb>
                                <Thumb>
                                    <Thumb.Template>
                                        <ControlTemplate TargetType="Thumb">
                                            <Border Background="#AA888888" CornerRadius="4" />
                                        </ControlTemplate>
                                    </Thumb.Template>
                                </Thumb>
                            </Track.Thumb>
                        </Track>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Setter Property="VerticalAlignment" Value="Bottom" />
        <Setter Property="HorizontalAlignment" Value="Right" />
    </Style>

    <!--  Style for the PropertyEditor  -->
    <Style TargetType="{x:Type local:PropertyEditor}">
        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:PropertyEditor}">
                    <Border Padding="{TemplateBinding Padding}" Background="Transparent" SnapsToDevicePixels="True">
                        <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                          VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                          Content="{TemplateBinding Content}"
                                          ContentTemplate="{TemplateBinding ContentTemplate}" />
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!--  Style for the CardPropertyEditor  -->
    <Style TargetType="{x:Type local:CardPropertyEditor}">
        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:CardPropertyEditor}">
                    <Border Padding="{TemplateBinding Padding}" Background="Transparent" SnapsToDevicePixels="True">
                        <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                          VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                          Content="{TemplateBinding Content}"
                                          ContentTemplate="{TemplateBinding ContentTemplate}" />
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!--  Style for DatePickerTextBox control  -->
    <Style TargetType="DatePickerTextBox">
        <Setter Property="Background" Value="{DynamicResource PropertyGridBackgroundBrush}" />
        <Setter Property="Foreground" Value="{DynamicResource PropertyGridForegroundBrush}" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Padding" Value="2,0" />
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="DatePickerTextBox">
                    <!--  Use a simple TextBox as the content element instead of ScrollViewer  -->
                    <Border Padding="{TemplateBinding Padding}"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}">
                        <Grid>
                            <TextBlock x:Name="PART_ContentElement"
                                       Margin="2,0,0,0"
                                       HorizontalAlignment="Left"
                                       VerticalAlignment="Center"
                                       Background="Transparent"
                                       Foreground="{DynamicResource PropertyGridForegroundBrush}"
                                       Text="{Binding Text, RelativeSource={RelativeSource TemplatedParent}}" />
                            <TextBlock x:Name="PART_Watermark"
                                       Margin="2,0,0,0"
                                       HorizontalAlignment="Left"
                                       VerticalAlignment="Center"
                                       Foreground="{DynamicResource PropertyGridDisabledForegroundBrush}"
                                       IsHitTestVisible="False"
                                       Text="Select Date"
                                       Visibility="Collapsed" />
                        </Grid>
                    </Border>
                    <ControlTemplate.Triggers>
                        <DataTrigger Binding="{Binding Text, RelativeSource={RelativeSource Self}}" Value="">
                            <Setter TargetName="PART_Watermark" Property="Visibility" Value="Visible" />
                        </DataTrigger>
                        <DataTrigger Binding="{Binding Text, RelativeSource={RelativeSource Self}}" Value="{x:Null}">
                            <Setter TargetName="PART_Watermark" Property="Visibility" Value="Visible" />
                        </DataTrigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!--  Style for DatePicker control  -->
    <Style TargetType="DatePicker">
        <Setter Property="Background" Value="{DynamicResource PropertyGridBackgroundBrush}" />
        <Setter Property="BorderBrush" Value="{DynamicResource PrimaryAccentBrush}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Foreground" Value="{DynamicResource PropertyGridForegroundBrush}" />
        <Setter Property="Height" Value="24" />
        <Setter Property="Padding" Value="4,2" />
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="CalendarStyle">
            <Setter.Value>
                <Style TargetType="Calendar">
                    <Setter Property="Background" Value="{DynamicResource PropertyGridBackgroundBrush}" />
                    <Setter Property="Foreground" Value="{DynamicResource PropertyGridForegroundBrush}" />
                </Style>
            </Setter.Value>
        </Setter>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="DatePicker">
                    <Border x:Name="PART_Root"
                            Padding="{TemplateBinding Padding}"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>

                            <!--  DatePickerTextBox part  -->
                            <DatePickerTextBox x:Name="PART_TextBox"
                                               Grid.Column="0"
                                               HorizontalAlignment="Stretch"
                                               VerticalAlignment="Center"
                                               Foreground="{TemplateBinding Foreground}" />

                            <!--  Calendar button  -->
                            <Button x:Name="PART_Button"
                                    Grid.Column="1"
                                    Width="20"
                                    Height="20"
                                    Margin="2,0,0,0"
                                    HorizontalAlignment="Center"
                                    VerticalAlignment="Center"
                                    Background="Transparent"
                                    BorderThickness="0"
                                    Focusable="False">
                                <TextBlock FontSize="12" Style="{StaticResource PropertyGridIconStyle}" Text="&#xE787;" />
                            </Button>

                            <!--  Popup for calendar  -->
                            <Popup x:Name="PART_Popup"
                                   AllowsTransparency="True"
                                   Placement="Bottom"
                                   PlacementTarget="{Binding ElementName=PART_Root}"
                                   StaysOpen="False" />
                        </Grid>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Foreground" Value="{DynamicResource PropertyGridDisabledForegroundBrush}" />
                            <Setter TargetName="PART_Button" Property="Foreground" Value="{DynamicResource PropertyGridDisabledForegroundBrush}" />
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="PART_Root" Property="BorderBrush" Value="{DynamicResource PrimaryAccentBrush}" />
                        </Trigger>
                        <Trigger Property="IsFocused" Value="True">
                            <Setter TargetName="PART_Root" Property="BorderBrush" Value="{DynamicResource PrimaryAccentBrush}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Resources>
            <!--  Global override for any text elements within DatePicker  -->
            <Style TargetType="Run">
                <Setter Property="Foreground" Value="{DynamicResource PropertyGridForegroundBrush}" />
            </Style>
            <Style TargetType="Span">
                <Setter Property="Foreground" Value="{DynamicResource PropertyGridForegroundBrush}" />
            </Style>
        </Style.Resources>
    </Style>

    <!--  Slider RepeatButton Style (must be defined before Slider style)  -->
    <Style x:Key="SliderRepeatButtonStyle" TargetType="RepeatButton">
        <Setter Property="OverridesDefaultStyle" Value="True" />
        <Setter Property="IsTabStop" Value="False" />
        <Setter Property="Focusable" Value="False" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="RepeatButton">
                    <Rectangle Fill="Transparent" />
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!--  Slider Thumb Style (must be defined before Slider style)  -->
    <Style x:Key="SliderThumbStyle" TargetType="Thumb">
        <Setter Property="Background" Value="{DynamicResource PropertyGridBorderBrush}" />
        <Setter Property="BorderBrush" Value="{DynamicResource PropertyGridBorderBrush}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Height" Value="14" />
        <Setter Property="Width" Value="14" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Thumb">
                    <Ellipse Fill="{TemplateBinding Background}" Stroke="{TemplateBinding BorderBrush}" StrokeThickness="{TemplateBinding BorderThickness}" />
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="{DynamicResource PropertyGridHoverBackgroundBrush}" />
            </Trigger>
            <Trigger Property="IsDragging" Value="True">
                <Setter Property="Background" Value="{DynamicResource PropertyGridSelectedBackgroundBrush}" />
            </Trigger>
        </Style.Triggers>
    </Style>

    <!--  PropertyGrid Slider Style (references the above styles)  -->
    <Style x:Key="PropertyGridSliderStyle" TargetType="Slider">
        <Setter Property="Background" Value="{DynamicResource PropertyGridSecondaryBackgroundBrush}" />
        <Setter Property="BorderBrush" Value="{DynamicResource PropertyGridBorderBrush}" />
        <Setter Property="Foreground" Value="{DynamicResource PropertyGridBorderBrush}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Slider">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" MinHeight="{TemplateBinding MinHeight}" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>
                        <TickBar x:Name="TopTick" Grid.Row="0" Visibility="Collapsed" />
                        <Border x:Name="TrackBackground"
                                Grid.Row="1"
                                Height="4"
                                Margin="5,0"
                                VerticalAlignment="Center"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="1" />
                        <Track x:Name="PART_Track" Grid.Row="1">
                            <Track.DecreaseRepeatButton>
                                <RepeatButton Command="Slider.DecreaseLarge" Style="{StaticResource SliderRepeatButtonStyle}" />
                            </Track.DecreaseRepeatButton>
                            <Track.Thumb>
                                <Thumb x:Name="Thumb" Style="{StaticResource SliderThumbStyle}" />
                            </Track.Thumb>
                            <Track.IncreaseRepeatButton>
                                <RepeatButton Command="Slider.IncreaseLarge" Style="{StaticResource SliderRepeatButtonStyle}" />
                            </Track.IncreaseRepeatButton>
                        </Track>
                        <TickBar x:Name="BottomTick" Grid.Row="2" Visibility="Collapsed" />
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
