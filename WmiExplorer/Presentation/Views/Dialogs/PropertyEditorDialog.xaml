<Window x:Class="WmiExplorer.Presentation.Views.Dialogs.PropertyEditorDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:behaviors="clr-namespace:WmiExplorer.Presentation.Behaviors"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:pg="clr-namespace:WmiExplorer.PropertyGrid;assembly=WmiExplorer.PropertyGrid"
        Width="600"
        Height="800"
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="CanResizeWithGrip"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None"
        mc:Ignorable="d">
    <Border Background="{DynamicResource PrimaryBackgroundBrush}"
            BorderBrush="{DynamicResource BorderBrush}"
            BorderThickness="1"
            CornerRadius="3">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <!--  Header Area  -->
            <Border Grid.Row="0"
                    Padding="16,12,16,12"
                    behaviors:WindowDragBehavior.IsDragEnabled="True"
                    Background="{DynamicResource PrimaryBackgroundBrush}"
                    BorderBrush="{DynamicResource BorderBrush}"
                    BorderThickness="0,0,0,1">
                <StackPanel Orientation="Vertical">

                    <StackPanel Orientation="Horizontal">
                        <ContentControl Margin="0,0,8,0" ContentTemplate="{StaticResource EditIconTemplate}" />
                        <TextBlock FontSize="18"
                                   FontWeight="SemiBold"
                                   Foreground="{DynamicResource PrimaryForegroundBrush}"
                                   Text="{Binding Title}" />
                    </StackPanel>

                    <TextBlock Margin="0,4,0,0"
                               FontSize="13"
                               Foreground="{DynamicResource SecondaryForegroundBrush}"
                               Text="{Binding ObjectTypeName, StringFormat=Type: {0}}"
                               Visibility="{Binding ObjectTypeName, Converter={StaticResource StringNotEmptyToVisibilityConverter}}" />

                    <!--  Accent underline  -->
                    <Rectangle Height="2"
                               Margin="0,8,0,0"
                               HorizontalAlignment="Stretch"
                               Fill="{DynamicResource PrimaryAccentBrush}" />
                </StackPanel>
            </Border>

            <!--  Property Grid Content  -->
            <Border Grid.Row="1"
                    Margin="16"
                    BorderBrush="{DynamicResource BorderBrush}"
                    BorderThickness="1"
                    CornerRadius="3">
                <pg:PropertyGrid IsPropertyGridReadOnly="False"
                                 NameColumnWidth="150"
                                 SelectedObject="{Binding EditableObject}"
                                 ShowHelpPane="True"
                                 ShowOptions="True"
                                 ShowSearchBox="True"
                                 ShowWmiOptions="False"
                                 UseCardStyle="True">
                    <pg:PropertyGrid.FilterOptions>
                        <pg:PropertyFilterOptions IncludeNullValues="True" IncludeReadOnlyProperties="False" IncludeSystemProperties="True" />
                    </pg:PropertyGrid.FilterOptions>
                </pg:PropertyGrid>
            </Border>

            <!--  Button Area  -->
            <Border Grid.Row="2"
                    Padding="16"
                    BorderBrush="{DynamicResource BorderBrush}"
                    BorderThickness="0,1,0,0">
                <StackPanel HorizontalAlignment="Right" Orientation="Horizontal">
                    <Button Margin="0,0,8,0" Command="{Binding CancelCommand}" Content="Cancel" />
                    <Button Command="{Binding OkCommand}" Content="OK" IsDefault="True" />
                </StackPanel>
            </Border>
        </Grid>
    </Border>
</Window>
