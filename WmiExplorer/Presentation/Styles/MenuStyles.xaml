<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <!--  Menu brushes now reference shared theme brushes  -->
    <!--  Uses SecondaryBackgroundBrush, BorderBrush, PrimaryForegroundBrush, PrimaryAccentBrush, SecondaryAccentBrush  -->
    <!--  Menu  -->
    <Style TargetType="Menu">
        <Setter Property="Foreground" Value="{DynamicResource PrimaryForegroundBrush}" />
        <Setter Property="Background" Value="{DynamicResource PrimaryBackgroundBrush}" />
        <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Padding" Value="{StaticResource PaddingTiny}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Menu}">
                    <Border Padding="{TemplateBinding Padding}"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            SnapsToDevicePixels="true">
                        <ItemsPresenter SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!--  Modern ToggleSwitch Style  -->
    <Style x:Key="MenuToggleSwitchStyle" TargetType="CheckBox">
        <Setter Property="Focusable" Value="False" />
        <Setter Property="Width" Value="28" />
        <Setter Property="Height" Value="14" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="CheckBox">
                    <Grid>
                        <Border x:Name="SwitchTrack"
                                Width="28"
                                Height="14"
                                Background="{TemplateBinding Background}"
                                CornerRadius="7" />
                        <Border x:Name="SwitchThumb"
                                Width="10"
                                Height="10"
                                Margin="2,2,0,2"
                                HorizontalAlignment="Left"
                                Background="{DynamicResource PrimaryForegroundBrush}"
                                CornerRadius="5"
                                RenderTransformOrigin="0.5,0.5">
                            <Border.RenderTransform>
                                <TranslateTransform x:Name="ThumbTransform" X="0" />
                            </Border.RenderTransform>
                        </Border>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="SwitchThumb" Property="Background" Value="{DynamicResource PrimaryForegroundBrush}" />
                            <Setter TargetName="SwitchThumb" Property="HorizontalAlignment" Value="Right" />
                            <Setter TargetName="SwitchThumb" Property="Margin" Value="0,2,2,2" />
                        </Trigger>
                        <Trigger Property="IsChecked" Value="False">
                            <Setter TargetName="SwitchTrack" Property="Background" Value="{DynamicResource DisabledBackgroundBrush}" />
                            <Setter TargetName="SwitchThumb" Property="Background" Value="{DynamicResource PrimaryForegroundBrush}" />
                            <Setter TargetName="SwitchThumb" Property="HorizontalAlignment" Value="Left" />
                            <Setter TargetName="SwitchThumb" Property="Margin" Value="2,2,0,2" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="SwitchTrack" Property="Background" Value="{DynamicResource BorderBrush}" />
                            <Setter TargetName="SwitchThumb" Property="Background" Value="{DynamicResource DisabledBackgroundBrush}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!--  Custom template for MenuItems that removes the white border around submenus and has proper submenu arrow indicators  -->
    <ControlTemplate x:Key="MenuItemControlTemplate" TargetType="{x:Type MenuItem}">
        <Border x:Name="templateRoot"
                Background="{TemplateBinding Background}"
                BorderBrush="{TemplateBinding BorderBrush}"
                BorderThickness="{TemplateBinding BorderThickness}"
                SnapsToDevicePixels="True">
            <Grid VerticalAlignment="Center">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition x:Name="IconColumn">
                        <ColumnDefinition.Width>
                            <MultiBinding Converter="{StaticResource TopLevelMenuIconColumnWidthConverter}">
                                <Binding Path="Role" RelativeSource="{RelativeSource TemplatedParent}" />
                                <Binding ElementName="Icon" Path="Content" />
                            </MultiBinding>
                        </ColumnDefinition.Width>
                    </ColumnDefinition>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition x:Name="ToggleColumn" Width="Auto" />
                    <ColumnDefinition x:Name="ArrowColumn" Width="Auto" />
                </Grid.ColumnDefinitions>
                <!--  Icon content if present  -->
                <ContentPresenter x:Name="Icon"
                                  Grid.Column="0"
                                  Width="16"
                                  Height="16"
                                  Margin="4,0,4,0"
                                  HorizontalAlignment="Center"
                                  VerticalAlignment="Center"
                                  ContentSource="Icon"
                                  SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
                <!--  Header content  -->
                <ContentPresenter Grid.Column="1"
                                  Margin="{TemplateBinding Padding}"
                                  VerticalAlignment="Center"
                                  ContentSource="Header"
                                  RecognizesAccessKey="True"
                                  SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
                <!--  Modern ToggleSwitch for checkable menu items  -->
                <CheckBox x:Name="ToggleSwitch"
                          Grid.Column="3"
                          Margin="8,0,8,0"
                          VerticalAlignment="Center"
                          Background="{DynamicResource BaseBlueBrush}"
                          IsChecked="{Binding IsChecked, RelativeSource={RelativeSource TemplatedParent}, Mode=TwoWay}"
                          Style="{StaticResource MenuToggleSwitchStyle}"
                          Visibility="{Binding IsCheckable, RelativeSource={RelativeSource TemplatedParent}, Converter={StaticResource BoolToVisibilityConverter}}" />
                <!--  Submenu arrow indicator  -->
                <Path x:Name="SubMenuArrow"
                      Grid.Column="4"
                      Width="4"
                      Height="8"
                      Margin="6,0,6,0"
                      HorizontalAlignment="Center"
                      VerticalAlignment="Center"
                      Data="M 0,0 L 4,4 L 0,8 Z"
                      Fill="{DynamicResource PrimaryForegroundBrush}"
                      Visibility="Collapsed" />
                <!--  Popup for submenu  -->
                <Popup x:Name="PART_Popup"
                       Margin="0"
                       AllowsTransparency="True"
                       Focusable="False"
                       HorizontalOffset="0"
                       IsOpen="{Binding IsSubmenuOpen, RelativeSource={RelativeSource TemplatedParent}}"
                       Placement="Right"
                       PlacementTarget="{Binding ElementName=templateRoot}"
                       PopupAnimation="Fade"
                       StaysOpen="False"
                       VerticalOffset="0">
                    <Border x:Name="SubMenuBorder"
                            MinWidth="175"
                            Padding="{StaticResource PaddingSmall}"
                            Background="{DynamicResource SecondaryBackgroundBrush}"
                            BorderBrush="{DynamicResource BorderBrush}"
                            BorderThickness="0"
                            SnapsToDevicePixels="True"
                            UseLayoutRounding="True">
                        <Border.Effect>
                            <DropShadowEffect BlurRadius="7"
                                              Direction="315"
                                              Opacity="0.3"
                                              ShadowDepth="3"
                                              Color="Black" />
                        </Border.Effect>
                        <ScrollViewer x:Name="SubMenuScrollViewer"
                                      Padding="0"
                                      BorderThickness="0"
                                      HorizontalScrollBarVisibility="Disabled"
                                      VerticalScrollBarVisibility="Auto">
                            <ItemsPresenter x:Name="ItemsPresenter"
                                            Margin="0"
                                            Grid.IsSharedSizeScope="True"
                                            KeyboardNavigation.DirectionalNavigation="Cycle"
                                            SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
                        </ScrollViewer>
                    </Border>
                </Popup>
            </Grid>
        </Border>
        <ControlTemplate.Triggers>
            <!--  Highlight on mouse over  -->
            <Trigger Property="IsMouseOver" Value="true">
                <Setter TargetName="templateRoot" Property="Background" Value="{DynamicResource HoverBackgroundBrush}" />
                <Setter TargetName="templateRoot" Property="BorderBrush" Value="{DynamicResource HoverBackgroundBrush}" />
                <Setter Property="Foreground" Value="{DynamicResource PrimaryForegroundBrush}" />
            </Trigger>
            <!--  Change appearance when menu item is pressed  -->
            <Trigger Property="IsPressed" Value="True">
                <Setter TargetName="templateRoot" Property="Background" Value="{DynamicResource PrimaryAccentBrush}" />
                <Setter TargetName="templateRoot" Property="BorderBrush" Value="{DynamicResource PrimaryAccentBrush}" />
                <Setter Property="Foreground" Value="{DynamicResource PrimaryForegroundBrush}" />
            </Trigger>
            <!--  Change appearance when menu item is enabled/disabled  -->
            <Trigger Property="IsEnabled" Value="False">
                <Setter Property="Foreground" Value="{DynamicResource BorderBrush}" />
                <Setter TargetName="templateRoot" Property="Opacity" Value="0.7" />
            </Trigger>
            <!--  Show submenu arrow for items with children  -->
            <Trigger Property="HasItems" Value="True">
                <Setter TargetName="SubMenuArrow" Property="Visibility" Value="Visible" />
            </Trigger>
            <!--  Handle top-level menu items differently (those in the main menu bar)  -->
            <Trigger Property="Role" Value="TopLevelHeader">
                <Setter Property="Padding" Value="{StaticResource PaddingMedium}" />
                <Setter TargetName="IconColumn" Property="Width" Value="0" />
                <Setter TargetName="ToggleColumn" Property="Width" Value="0" />
                <Setter TargetName="templateRoot" Property="Margin" Value="{StaticResource MarginHorizontalTiny}" />
                <Setter TargetName="PART_Popup" Property="Placement" Value="Bottom" />
                <Setter TargetName="SubMenuArrow" Property="Visibility" Value="Collapsed" />
            </Trigger>
            <!--  Handle top-level menu items without children  -->
            <Trigger Property="Role" Value="TopLevelItem">
                <Setter Property="Padding" Value="{StaticResource PaddingMedium}" />
                <!-- <Setter TargetName="IconColumn" Property="Width" Value="Auto" /> -->
                <Setter TargetName="ToggleColumn" Property="Width" Value="0" />
                <Setter TargetName="templateRoot" Property="Margin" Value="{StaticResource MarginHorizontalTiny}" />
                <Setter TargetName="SubMenuArrow" Property="Visibility" Value="Collapsed" />
            </Trigger>
            <!--  Handle submenu headers differently  -->
            <Trigger Property="Role" Value="SubmenuHeader">
                <Setter Property="Padding" Value="{StaticResource PaddingMedium}" />
                <Setter Property="MinWidth" Value="150" />
                <Setter TargetName="PART_Popup" Property="Placement" Value="Right" />
                <Setter TargetName="PART_Popup" Property="HorizontalOffset" Value="1" />
                <Setter TargetName="PART_Popup" Property="VerticalOffset" Value="0" />
            </Trigger>
            <!--  Make sure top-level menu items look good when selected  -->
            <MultiTrigger>
                <MultiTrigger.Conditions>
                    <Condition Property="IsHighlighted" Value="True" />
                    <Condition Property="Role" Value="TopLevelHeader" />
                </MultiTrigger.Conditions>
                <Setter TargetName="templateRoot" Property="Background" Value="{DynamicResource PrimaryAccentBrush}" />
                <Setter TargetName="templateRoot" Property="BorderBrush" Value="{DynamicResource PrimaryAccentBrush}" />
                <Setter Property="Foreground" Value="{DynamicResource PrimaryForegroundBrush}" />
            </MultiTrigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>

    <!--  Style for ContextMenu to remove border  -->
    <Style TargetType="ContextMenu">
        <Setter Property="Background" Value="{DynamicResource SecondaryBackgroundBrush}" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Padding" Value="{StaticResource PaddingTiny}" />
        <Setter Property="MinWidth" Value="175" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ContextMenu">
                    <Border Padding="{StaticResource PaddingSmall}"
                            Background="{DynamicResource SecondaryBackgroundBrush}"
                            BorderBrush="{DynamicResource BorderBrush}"
                            BorderThickness="0"
                            UseLayoutRounding="True">
                        <Border.Effect>
                            <DropShadowEffect BlurRadius="7"
                                              Direction="315"
                                              Opacity="0.3"
                                              ShadowDepth="3"
                                              Color="Black" />
                        </Border.Effect>
                        <ItemsPresenter Margin="0" />
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!--  Style for all MenuItem elements  -->
    <Style TargetType="MenuItem">
        <Setter Property="Padding" Value="{StaticResource PaddingMedium}" />
        <Setter Property="Foreground" Value="{DynamicResource PrimaryForegroundBrush}" />
        <Setter Property="Background" Value="{DynamicResource PrimaryBackgroundBrush}" />
        <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Template" Value="{StaticResource MenuItemControlTemplate}" />
        <Setter Property="Margin" Value="{StaticResource MarginRightTiny}" />
    </Style>

</ResourceDictionary>
