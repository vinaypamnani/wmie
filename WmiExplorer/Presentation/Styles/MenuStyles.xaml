<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <!--  Menu brushes now reference shared theme brushes  -->
    <!--  Uses SecondaryBackgroundBrush, BorderBrush, PrimaryForegroundBrush, PrimaryAccentBrush, SecondaryAccentBrush  -->
    <!--  Menu  -->
    <Style TargetType="Menu">
        <Setter Property="Foreground" Value="{DynamicResource PrimaryForegroundBrush}" />
        <Setter Property="Background" Value="{DynamicResource SecondaryBackgroundBrush}" />
        <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Padding" Value="{StaticResource PaddingTiny}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Menu}">
                    <Border
                        Padding="{TemplateBinding Padding}"
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        SnapsToDevicePixels="true">
                        <ItemsPresenter SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!--  Custom template for MenuItems that removes the white border around submenus  -->
    <ControlTemplate x:Key="MenuItemControlTemplate" TargetType="{x:Type MenuItem}">
        <Border
            x:Name="templateRoot"
            Background="{TemplateBinding Background}"
            BorderBrush="{TemplateBinding BorderBrush}"
            BorderThickness="{TemplateBinding BorderThickness}"
            SnapsToDevicePixels="True">
            <Grid VerticalAlignment="Center">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition x:Name="IconColumn" Width="0" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <!--  Icon content if present  -->
                <ContentPresenter
                    x:Name="Icon"
                    Width="16"
                    Height="16"
                    Margin="0"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    ContentSource="Icon"
                    SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
                <!--  Header content  -->
                <ContentPresenter
                    Grid.Column="1"
                    Margin="{TemplateBinding Padding}"
                    ContentSource="Header"
                    RecognizesAccessKey="True"
                    SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
                <!--  Popup for submenu  -->
                <Popup
                    x:Name="PART_Popup"
                    Margin="0"
                    AllowsTransparency="True"
                    Focusable="False"
                    HorizontalOffset="0"
                    IsOpen="{Binding IsSubmenuOpen, RelativeSource={RelativeSource TemplatedParent}}"
                    Placement="Bottom"
                    PopupAnimation="None"
                    StaysOpen="False">
                    <Border
                        x:Name="SubMenuBorder"
                        MinWidth="175"
                        Padding="{StaticResource PaddingSmall}"
                        Background="{DynamicResource SecondaryBackgroundBrush}"
                        BorderBrush="{DynamicResource BorderBrush}"
                        BorderThickness="0"
                        SnapsToDevicePixels="True"
                        UseLayoutRounding="True">
                        <Border.Effect>
                            <DropShadowEffect
                                BlurRadius="7"
                                Direction="315"
                                Opacity="0.3"
                                ShadowDepth="3"
                                Color="Black" />
                        </Border.Effect>
                        <ScrollViewer
                            x:Name="SubMenuScrollViewer"
                            Padding="0"
                            BorderThickness="0"
                            HorizontalScrollBarVisibility="Disabled"
                            VerticalScrollBarVisibility="Auto">
                            <ItemsPresenter
                                x:Name="ItemsPresenter"
                                Margin="0"
                                Grid.IsSharedSizeScope="True"
                                KeyboardNavigation.DirectionalNavigation="Cycle"
                                SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
                        </ScrollViewer>
                    </Border>
                </Popup>
            </Grid>
        </Border>
        <ControlTemplate.Triggers>
            <!--  Highlight on mouse over  -->
            <Trigger Property="IsMouseOver" Value="true">
                <Setter TargetName="templateRoot" Property="Background" Value="{DynamicResource PrimaryAccentBrush}" />
                <Setter TargetName="templateRoot" Property="BorderBrush" Value="{DynamicResource PrimaryAccentBrush}" />
                <Setter Property="Foreground" Value="{DynamicResource PrimaryForegroundBrush}" />
            </Trigger>
            <!--  Change appearance when menu item is pressed  -->
            <Trigger Property="IsPressed" Value="True">
                <Setter TargetName="templateRoot" Property="Background" Value="{DynamicResource SecondaryAccentBrush}" />
                <Setter TargetName="templateRoot" Property="BorderBrush" Value="{DynamicResource SecondaryAccentBrush}" />
                <Setter Property="Foreground" Value="{DynamicResource PrimaryForegroundBrush}" />
            </Trigger>
            <!--  Change appearance when menu item is enabled/disabled  -->
            <Trigger Property="IsEnabled" Value="False">
                <Setter Property="Foreground" Value="{DynamicResource BorderBrush}" />
                <Setter TargetName="templateRoot" Property="Opacity" Value="0.7" />
            </Trigger>
            <!--  Handle top-level menu items differently (those in the main menu bar)  -->
            <Trigger Property="Role" Value="TopLevelHeader">
                <Setter Property="Padding" Value="{StaticResource PaddingMedium}" />
                <Setter TargetName="IconColumn" Property="Width" Value="0" />
                <Setter TargetName="templateRoot" Property="Margin" Value="{StaticResource MarginHorizontalTiny}" />
            </Trigger>
            <!--  Handle top-level menu items without children  -->
            <Trigger Property="Role" Value="TopLevelItem">
                <Setter Property="Padding" Value="{StaticResource PaddingMedium}" />
                <Setter TargetName="IconColumn" Property="Width" Value="0" />
                <Setter TargetName="templateRoot" Property="Margin" Value="{StaticResource MarginHorizontalTiny}" />
            </Trigger>
            <!--  Handle submenu headers differently  -->
            <Trigger Property="Role" Value="SubmenuHeader">
                <Setter Property="Padding" Value="{StaticResource PaddingMedium}" />
                <Setter Property="MinWidth" Value="150" />
            </Trigger>
            <!--  Make sure top-level menu items look good when selected  -->
            <MultiTrigger>
                <MultiTrigger.Conditions>
                    <Condition Property="IsHighlighted" Value="True" />
                    <Condition Property="Role" Value="TopLevelHeader" />
                </MultiTrigger.Conditions>
                <Setter TargetName="templateRoot" Property="Background" Value="{DynamicResource PrimaryAccentBrush}" />
                <Setter TargetName="templateRoot" Property="BorderBrush" Value="{DynamicResource PrimaryAccentBrush}" />
                <Setter Property="Foreground" Value="{DynamicResource PrimaryForegroundBrush}" />
            </MultiTrigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>
    <!--  Style for ContextMenu to remove border  -->
    <Style TargetType="ContextMenu">
        <Setter Property="Background" Value="{DynamicResource SecondaryBackgroundBrush}" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Padding" Value="{StaticResource PaddingTiny}" />
        <Setter Property="MinWidth" Value="175" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ContextMenu">
                    <Border
                        Padding="{StaticResource PaddingSmall}"
                        Background="{DynamicResource SecondaryBackgroundBrush}"
                        BorderBrush="{DynamicResource BorderBrush}"
                        BorderThickness="0"
                        UseLayoutRounding="True">
                        <Border.Effect>
                            <DropShadowEffect
                                BlurRadius="7"
                                Direction="315"
                                Opacity="0.3"
                                ShadowDepth="3"
                                Color="Black" />
                        </Border.Effect>
                        <ItemsPresenter Margin="0" />
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    <!--  Style for all MenuItem elements  -->
    <Style TargetType="MenuItem">
        <Setter Property="Padding" Value="{StaticResource PaddingMedium}" />
        <Setter Property="Foreground" Value="{DynamicResource PrimaryForegroundBrush}" />
        <Setter Property="Background" Value="{DynamicResource SecondaryBackgroundBrush}" />
        <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Template" Value="{StaticResource MenuItemControlTemplate}" />
        <Setter Property="Margin" Value="{StaticResource MarginRightTiny}" />
    </Style>

</ResourceDictionary>
